apiVersion: v1
kind: Service
metadata: 
  name: app-service
  labels:
    app: petclinic
    environment: dev
  namespace: petclinic
spec:
  type: LoadBalancer
  selector:
    app: petclinic
  ports:
  - name: app
    protocol: "TCP"
    port: 8080
    targetPort: 8080    
